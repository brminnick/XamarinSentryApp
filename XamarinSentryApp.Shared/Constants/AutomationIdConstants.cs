using System;

namespace XamarinSentryApp.Shared
{
    public static class AutomationIdConstants
    {
        public const string Stepper = nameof(Stepper);
        public const string CountLabel = nameof(CountLabel);
    }
}
